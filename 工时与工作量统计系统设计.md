# 工时与工作量统计系统设计文档

> 版本:v1.2 · 配套程序:研发项目工作台.html v3.8
> 定位:在现有「图书馆研发工作台」(看板/甘特图/任务/统计)基础上,新增**任务级工时登记与工作量统计**能力,用于项目进度管理。

---

## 1. 需求概述

| 能力 | 说明 |
|------|------|
| 项目管理 | 新建项目:名称、开始时间、截止时间(仅作时间约束)、预估工作量(计划工时) |
| 工时登记 | 每位成员按「记录人 + 工时数 + 对应项目/任务」登记实际工时,可带备注;**自动记录登记日期时间** |
| 任务级登记 | 每个任务卡片提供「记工时」按钮,填数字 → 确认保存,工时归属该任务并汇总到所属项目 |
| 工时统计 | 自动汇总计划/实际工时;按项目、按人员双维度对比分析 |
| 已用工作日 | 项目「已用工作日」= 累计登记工时 ÷ **7.5 小时**(无登记时按日期推算) |
| 偏差分析 | 展示计划 vs 实际偏差,判断是否超支、是否滞后 |
| 进度计算 | 工作量百分比(实际/计划×100%)+ 综合时间进度的工作进度规则 |
| 任务完成 | 任务卡片提供「完成」按钮(非勾选),点击切换任务完成状态 |
| 可视化 | 进度条、百分比、超支/滞后标记 |

---

## 2. 数据结构

### 2.1 项目(projects,沿用现有字段)

```js
{
  id: 'id_xxx',
  name: '项目名称',
  category: '新产品开发 | 老产品优化 | 非标项目',
  status: '进行中 | 已交付 | 已暂停',
  priority: 'P0 | P1 | P2',
  owner: '负责人',
  startDate: '2026-08-01',      // 仅作时间约束(算时间进度参考)
  endDate:   '2026-10-01',      // 仅作时间约束(算时间进度参考)
  progress: 40,                  // 工作进度%(见 §3.2 计算规则)
  workDaysPlanned: 65,           // 计划工时(人天)。自动模式=开始→截止工作日数;可手动改
  workDaysUsed: 26,              // 已用工作日(人天),自动模式=登记工时÷7.5 折算;无登记按日期;可手动改
  autoProgress: true, autoWorkDays: true,   // 自动计算开关
  desc: '…', milestones: [ {name,date} ]
}
```

> 单位约定:**计划/已用工时按「人天」**;工时登记按「小时」录入。换算关系 **1 人天 = 7.5 小时**。

### 2.2 工时登记(worklogs,新增字段)

```js
worklogs: [
  {
    id: 'wl_xxx',
    projectId: 'id_xxx',        // 所属项目(必填)
    taskId: 'id_yyy',           // 关联任务(任务级登记时必填;项目级登记为空)
    person: '张三',              // 记录人
    date: '2026-08-12',         // 登记日期 YYYY-MM-DD
    createdAt: '2026-08-12 14:35', // 登记日期时间(精确到分钟,自动记录)
    hours: 4,                   // 工时数(小时,支持 0.5 步进,单次 ≤24)
    note: '联调接口'             // 可选备注
  }
]
```

- 存储:localStorage key `wb_lib_worklog_v2`;后端 `/api/data` 的 JSON 整体随 `{projects, tasks, worklogs}` 同步,多设备一致。
- 同一项目/任务可多条记录、同一人可多条记录;查询时按条件汇总。
- **二次编辑**:每条记录可通过明细列表「编辑」按钮修改(记录人/日期/工时数/备注/所属项目),`createdAt` 登记时间保持不变;项目级编辑改所属项目时,新旧两个项目的进度与已用工作日都会重算。
- 老数据(无 taskId/createdAt)兼容:显示时回退到 `date`,不影响统计。

### 2.3 派生数据(不落库,实时计算)

| 名称 | 定义 |
|------|------|
| `hoursUsed(项目)` | Σ worklogs[该项目].hours,登记工时合计(小时) |
| `hoursUsed(任务)` | Σ worklogs[该任务].hours,任务登记工时合计(小时) |
| `hoursPlanned(项目)` | workDaysPlanned × 7.5(计划小时) |
| `workDaysUsed(项目)` | 有登记:hoursUsed ÷ 7.5(保留 1 位小数);无登记:开始→min(今天,截止)工作日数 |
| `hoursByPerson(人员)` | Σ worklogs[该人].hours |

---

## 3. 计算逻辑(重点)

### 3.1 项目维度

#### ① 工作量进度 E(工时完成度,纯工时口径)
```
E = min( 登记工时合计 / (计划工时人天 × 7.5) , 100% )
```
- 反映"干了的活占预估工作量的比例",与日期无关 → 满足"实际工时/计划工时 ×100%"。
- 无计划工时(≤0):E 取 null(不参与综合)。

#### ② 时间进度 T(仅作参考约束,不进入工作量口径)
```
T = clamp( (今天 - 开始) / (截止 - 开始) , 0%, 100% )
```
- 无起止日期:T = null。**截止时间只用于计算 T 这一参考指标,不参与 E 的计算**。

#### ③ 综合工作进度 P(写入项目 progress 的规则)
```
有登记工时 且 有计划工时 : P = round( E×70% + T×30% )
无登记工时 但有任务      : P = 已完成任务数 / 总任务数 × 100%   (沿用现有)
无登记工时 且 无任务      : P = T (无 T 则为 0)
```
- 权重 70/30 的理由:工时是"干出来的"硬证据,权重高;时间进度起节奏校正作用,避免仅凭工时刻度失真(例如前期只做调研、后期集中编码时,工时占比与时间消耗会错位)。
- 权重常量集中定义(`PROG_W_E=0.7, PROG_W_T=0.3`),便于调整。

#### ④ 偏差指标(超支/滞后判断)
```
工时消耗率  C = 登记工时小时 / 计划小时 × 100%        (可 >100%)
工时偏差  ΔH = 登记小时 - 计划小时                    (>0 → 超支)
进度偏差  ΔP = E - T                                  (>0 → 超前,<0 → 滞后)
```

**预警规则**
| 条件 | 标记 |
|------|------|
| C > 100% | 🔴 超支 X%(红色) |
| ΔP < -15 且 E < 100 | 🟠 进度滞后(时间走了 60%,活只干了 40%) |
| 其余 | 🟢 正常 |

### 3.2 人员维度

```
人员登记工时 = Σ worklogs[person].hours
人员参与项目数 = 去重 worklogs[person].projectId 数量
```
- 人员维度只统计"实际登记",计划工时按项目归属,不强行分摊到人(避免拍脑袋);如需人均计划可后续按项目负责人归属扩展。

### 3.3 全局维度
```
总计划小时 = Σ 项目计划人天 × 7.5
总登记小时 = Σ worklogs.hours
全局消耗率 = 总登记小时 / 总计划小时 × 100%
```

---

## 4. 页面 / 功能模块划分

| 模块 | 位置 | 内容 |
|------|------|------|
| 记工时(项目级) | 顶部工具栏「记工时」按钮 | 打开登记弹窗:项目下拉、记录人(含常用姓名快捷选择)、日期(默认今天)、工时数(0.5 步进)、备注 |
| 记工时(任务级) | 任务卡片「⏱ 记工时」按钮(任务页 / 项目详情) | 弹窗:任务名、所属项目、**工时数(填数字,必填)**、记录人(默认任务负责人)、备注;确认保存后**自动记录登记日期时间**,并实时刷新该任务已登记合计 |
| 工时登记弹窗 | modalOverlay 复用 | 表单 + 最近登记记录列表(含登记时间,可**编辑**或删除误登记) |
| 任务工时徽标 | 任务卡片右上角 | 已登记工时显示为「⏱ X.Xh」,悬停可见折算工作日 |
| 已用工作日叠加 | 项目卡片/详情/编辑弹窗 | 已用工作日按登记工时 ÷7.5 折算显示,并提示「登记 X 小时 ≈ Y 天」 |
| 任务完成按钮 | 任务卡片 | 「完成」按钮(绿色实心)→ 点击变「已完成 ✓」;再点恢复,非勾选形式 |
| 工时总览 | 统计视图顶部 | 总登记工时 / 总计划小时 / 全局消耗率进度条 / 登记人次 |
| 人员工时排行 | 统计视图 | 按人员横向条形图:登记工时 + 参与项目数(与现有"按任务统计"并列互补) |
| 项目工时对比 | 统计视图项目卡片 | 计划 vs 登记双条对比、工作量进度 E、时间进度 T、综合进度 P、超支/滞后标记 |
| 登记明细入口 | 项目编辑弹窗 | 「已登记工时 X 小时(≈Y 个工作日)」只读汇总 + 「查看明细」列表(含登记时间) |
| Excel 导出 | 顶部「Excel」按钮 | 新增「工时登记」工作表(项目/关联任务/人员/登记日期/登记时间/工时/折算工作日/备注) |

---

## 5. 代码结构建议

### 5.1 函数分层(单文件 HTML 内嵌 JS)

```
── 数据层 ──
loadWorklogs()            从 localStorage 载入 worklogs
saveLocal()               写入 projects/tasks/worklogs(已有函数扩展)
_pushToServer()           POST {projects, tasks, worklogs}(已有函数扩展)
syncFromServer()          拉取时合并 worklogs(已有函数扩展)

── 计算层(纯函数,可单测)──
hoursUsedOfProject(pid)   项目登记工时合计(小时)
hoursUsedOfTask(tid)      任务登记工时合计(小时)
hoursPlannedOf(p)         计划小时 = workDaysPlanned × 7.5
calcEffortPct(p)          E 工作量进度
calcTimePct(p)            T 时间进度
calcBlendedPct(p)         P 综合工作进度
autoCalcWorkDaysUsed(p)   已用工作日 = 登记工时÷7.5;无登记按日期推算
worklogHoursByPerson()    人员工时汇总 Map
worklogListByProject(pid) 项目工时明细
worklogListByTask(tid)    任务工时明细
nowStamp()                登记时间戳 YYYY-MM-DD HH:mm
fmtHours(h)               小时/天数格式化(整数不带小数,否则 1 位)

── UI 层 ──
openTaskWorklogModal(tid, wid?) 打开任务级工时登记弹窗(wid 传入=二次编辑,预填原数据)
saveTaskWorklog()         任务级保存(自动记录 createdAt;编辑模式更新原记录,保留登记时间)+ saveData(true) 即时同步
renderTaskWorklogRecent() 弹窗内该任务已登记明细(含编辑/删除)
openWorklogModal(pid?, wid?) 打开项目级登记弹窗(可从项目详情带出项目;wid 传入=二次编辑)
saveWorklog()             项目级保存(同样记录 createdAt;编辑模式更新原记录,改项目时重算新旧两项目)
editWorklog(wid)          二次编辑入口:任务级记录→任务弹窗,项目级记录→项目弹窗
delWorklog(wid)           删除误登记(删除后自动刷新打开中的弹窗)
renderWorklogRecent()     弹窗内最近记录
toggleTask(tid)           完成/重新打开任务(按钮触发,非勾选)
renderStatsView()         统计视图(集成工时总览/人员排行/项目对比)
buildWorklogRows()        Excel「工时登记」工作表行
```

### 5.2 文件布局

```
E:\感创\研发工作台\
├─ 研发项目工作台.html        主程序(单文件,全部逻辑内嵌) v3.8
├─ 工时与工作量统计系统设计.md 本设计文档
├─ server\public\index.html   本地后台 + Railway 部署副本
└─ deploy\index.html          CloudStudio 部署副本
```

### 5.3 建议的后续扩展

1. 人员维度计划工时(按负责人归属分摊计划)
2. 工时日报/周报视图(按日期聚合)
3. 登记防重:同一人同一天同一项目合并提示
4. 导出增加"按人员/按月份"汇总透视表
5. 后端 `server.js` 增加只读统计接口(可选)
