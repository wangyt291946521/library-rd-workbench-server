# 工时与工作量统计系统设计文档

> 版本:v1.0 · 配套程序:研发项目工作台.html v3.5
> 定位:在现有「图书馆研发工作台」(看板/甘特图/任务/统计)基础上,新增**工时登记与工作量统计**能力,用于项目进度管理。

---

## 1. 需求概述

| 能力 | 说明 |
|------|------|
| 项目管理 | 新建项目:名称、开始时间、截止时间(仅作时间约束)、预估工作量(计划工时) |
| 工时登记 | 每位成员按「记录人 + 日期 + 工时数 + 对应项目」登记实际工时,可带备注 |
| 工时统计 | 自动汇总计划/实际工时;按项目、按人员双维度对比分析 |
| 偏差分析 | 展示计划 vs 实际偏差,判断是否超支、是否滞后 |
| 进度计算 | 工作量百分比(实际/计划×100%)+ 综合时间进度的工作进度规则 |
| 可视化 | 进度条、百分比、超支/滞后标记 |

---

## 2. 数据结构

### 2.1 项目(projects,沿用现有字段)

```js
{
  id: 'id_xxx',
  name: '项目名称',
  category: '新产品开发 | 老产品优化 | 非标项目',
  status: '进行中 | 已交付 | 已暂停',
  priority: 'P0 | P1 | P2',
  owner: '负责人',
  startDate: '2026-08-01',      // 仅作时间约束(算时间进度参考)
  endDate:   '2026-10-01',      // 仅作时间约束(算时间进度参考)
  progress: 40,                  // 工作进度%(见 §3.2 计算规则)
  workDaysPlanned: 65,           // 计划工时(人天)。自动模式=开始→截止工作日数;可手动改
  workDaysUsed: 26,              // 已用工作日(人天),自动模式=登记工时合计÷7.5;可手动改
  autoProgress: true, autoWorkDays: true,   // 自动计算开关
  desc: '…', milestones: [ {name,date} ]
}
```

> 单位约定:**计划/已用工时按「人天」**;工时登记按「小时」录入。换算关系 **1 人天 = 7.5 小时**。
>
> **已用工作日联动规则(自动模式)**:项目「已用工作日」= 该项目全部登记工时之和 ÷ 7.5,随每次工时登记/删除自动重算;无任何登记时为 0 天。手动模式则完全由用户填写,与登记无关。

### 2.2 工时登记(worklogs,新增)

```js
worklogs: [
  {
    id: 'wl_xxx',
    projectId: 'id_xxx',   // 所属项目
    person: '张三',          // 记录人
    date: '2026-08-12',     // 登记日期 YYYY-MM-DD
    hours: 4,               // 工时数(小时,支持 0.5 步进)
    note: '联调接口'         // 可选备注
  }
]
```

- 存储:localStorage key `wb_lib_worklog_v2`;后端 `/api/data` 的 JSON 整体随 `{projects, tasks, worklogs}` 同步,多设备一致。
- 同一项目可多条记录、同一人可多条记录;查询时按条件汇总。

### 2.3 派生数据(不落库,实时计算)

| 名称 | 定义 |
|------|------|
| `hoursUsed(项目)` | Σ worklogs[该项目].hours,登记工时合计(小时) |
| `hoursPlanned(项目)` | workDaysPlanned × 7.5(计划小时) |
| `hoursByPerson(人员)` | Σ worklogs[该人].hours |

---

## 3. 计算逻辑(重点)

### 3.1 项目维度

#### ① 工作量进度 E(工时完成度,纯工时口径)
```
E = min( 登记工时合计 / (计划工时人天 × 7.5) , 100% )
```
- 反映"干了的活占预估工作量的比例",与日期无关 → 满足"实际工时/计划工时 ×100%"。
- 无计划工时(≤0):E 取 null(不参与综合)。

#### ② 时间进度 T(仅作参考约束,不进入工作量口径)
```
T = clamp( (今天 - 开始) / (截止 - 开始) , 0%, 100% )
```
- 无起止日期:T = null。**截止时间只用于计算 T 这一参考指标,不参与 E 的计算**。

#### ③ 综合工作进度 P(写入项目 progress 的规则)
```
有登记工时 且 有计划工时 : P = round( E×70% + T×30% )
无登记工时 但有任务      : P = 已完成任务数 / 总任务数 × 100%   (沿用现有)
无登记工时 且 无任务      : P = T (无 T 则为 0)
```
- 权重 70/30 的理由:工时是"干出来的"硬证据,权重高;时间进度起节奏校正作用,避免仅凭工时刻度失真(例如前期只做调研、后期集中编码时,工时占比与时间消耗会错位)。
- 权重常量建议集中定义(如 `PROG_W_E=0.7, PROG_W_T=0.3`),便于调整。

#### ④ 偏差指标(超支/滞后判断)
```
工时消耗率  C = 登记工时小时 / 计划小时 × 100%        (可 >100%)
工时偏差  ΔH = 登记小时 - 计划小时                    (>0 → 超支)
进度偏差  ΔP = E - T                                  (>0 → 超前,<0 → 滞后)
```

**预警规则**
| 条件 | 标记 |
|------|------|
| C > 100% | 🔴 超支 X%(红色) |
| ΔP < -15 且 E < 100 | 🟠 进度滞后(时间走了 60%,活只干了 40%) |
| 其余 | 🟢 正常 |

### 3.2 人员维度

```
人员登记工时 = Σ worklogs[person].hours
人员参与项目数 = 去重 worklogs[person].projectId 数量
```
- 人员维度只统计"实际登记",计划工时按项目归属,不强行分摊到人(避免拍脑袋);如需人均计划可后续按项目负责人归属扩展。

### 3.3 全局维度
```
总计划小时 = Σ 项目计划人天 × 7.5
总登记小时 = Σ worklogs.hours
全局消耗率 = 总登记小时 / 总计划小时 × 100%
```

---

## 4. 页面 / 功能模块划分

| 模块 | 位置 | 内容 |
|------|------|------|
| 记工时 | 顶部工具栏「记工时」按钮 | 打开登记弹窗:项目下拉、记录人(含常用姓名快捷选择)、日期(默认今天)、工时数(0.5 步进)、备注 |
| 工时登记弹窗 | modalOverlay 复用 | 表单 + 最近登记记录列表(可删除误登记) |
| 工时总览 | 统计视图顶部 | 总登记工时 / 总计划小时 / 全局消耗率进度条 / 登记人次 |
| 人员工时排行 | 统计视图 | 按人员横向条形图:登记工时 + 参与项目数(与现有"按任务统计"并列互补) |
| 项目工时对比 | 统计视图项目卡片 | 计划 vs 登记双条对比、工作量进度 E、时间进度 T、综合进度 P、超支/滞后标记 |
| 登记明细入口 | 项目编辑弹窗 | 「已登记工时 X 小时」只读汇总 + 「查看明细」列表 |
| Excel 导出 | 顶部「Excel」按钮 | 新增「工时登记」工作表(项目/人员/日期/工时/备注) |

---

## 5. 代码结构建议

### 5.1 函数分层(单文件 HTML 内嵌 JS)

```
── 数据层 ──
loadWorklogs()            从 localStorage 载入 worklogs
saveLocal()               写入 projects/tasks/worklogs(已有函数扩展)
_pushToServer()           POST {projects, tasks, worklogs}(已有函数扩展)
syncFromServer()          拉取时合并 worklogs(已有函数扩展)

── 计算层(纯函数,可单测)──
hoursUsedOfProject(pid)   登记工时合计(小时)
hoursPlannedOf(p)         计划小时 = workDaysPlanned × 7.5
calcEffortPct(p)          E 工作量进度
calcTimePct(p)            T 时间进度
calcBlendedPct(p)         P 综合工作进度
worklogHoursByPerson()    人员工时汇总 Map
worklogListByProject(pid) 项目工时明细

── UI 层 ──
openWorklogModal(pid?)    打开登记弹窗(可从项目详情带出项目)
saveWorklog()             保存登记 + saveData(true) 即时同步
delWorklog(wid)           删除误登记
renderWorklogRecent()     弹窗内最近记录
renderStatsView()         统计视图(集成工时总览/人员排行/项目对比)
buildWorklogRows()        Excel「工时登记」工作表行
```

### 5.2 文件布局

```
E:\感创\研发工作台\
├─ 研发项目工作台.html        主程序(单文件,全部逻辑内嵌)
├─ 工时与工作量统计系统设计.md 本设计文档
├─ server\public\index.html   本地后台 + Railway 部署副本
└─ deploy\index.html          CloudStudio 部署副本
```

### 5.3 建议的后续扩展

1. 人员维度计划工时(按负责人归属分摊计划)
2. 工时日报/周报视图(按日期聚合)
3. 登记防重:同一人同一天同一项目合并提示
4. 导出增加"按人员/按月份"汇总透视表
5. 后端 `server.js` 增加只读统计接口(可选)
