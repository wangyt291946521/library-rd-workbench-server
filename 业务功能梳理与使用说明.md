# 图书馆研发项目工作台 · 业务功能梳理与使用说明

> 版本 v3.8 · 面向产品经理/项目负责人的日常使用指南
> 一句话定位:把「项目、任务、工时」装进一个页面,自动算进度、自动算工作量、自动发现超支和滞后。

---

## 一、这个系统能帮你做什么

| 你的问题 | 系统给出的答案 |
|---------|--------------|
| 现在手头有几个项目?进行到哪了? | 看板视图:三列分类,卡片一眼看全 |
| 每个项目什么时候开始、什么时候交付? | 甘特图:时间轴排布,防撞期 |
| 谁在做什么、卡在哪里? | 任务列表:按负责人筛选 |
| 团队实际投入了多少工时?和计划差多少? | 统计视图:人员排行 + 项目偏差分析 |
| 项目到底算完成了百分之多少? | 自动进度:优先按工时算,其次按任务,最后按时间 |
| 月底要给领导汇报? | 一键导出 Excel(5 个工作表) |

---

## 二、四个视图怎么用

### 1. 看板(默认首页)
- **三列**:新产品开发 / 老产品优化 / 非标项目
- **同列拖拽排序**:鼠标按住卡片上下拖动即可调整展示顺序(手机端按住卡片右上角 `⠿` 手柄),松手自动保存并同步
- 点卡片 → 弹出项目详情(进度、工时、节点、任务)

### 2. 甘特图
- 按开始/截止日期画时间条,横向滚动查看整年
- 顶部有月份/周刻度,当前日期有红线标识
- 没填日期的项目不显示(避免空条)

### 3. 任务列表
- 支持**状态筛选 + 姓名筛选**同时生效(如:只看「进行中」且是「张三」的任务)
- 每个任务卡片有四个按钮:**⏱ 记工时** / **完成**(或「已完成 ✓」) / 编辑 / 删除
- 卡片右上角显示该任务已登记工时「⏱ X.Xh」

### 4. 统计视图(最核心)
从上到下依次是:
1. **总览卡片**:项目数 / 完成率 / 交付率 / 参与人数
2. **工时总览**:登记工时合计 / 计划工时合计 / 全局消耗率 / 登记笔数
3. **人员工时排行**:每人横向条形图(登记工时 + 参与项目数)
4. **项目任务完成**:每个项目的任务完成进度条
5. **项目工时对比**:每个项目的完整分析(见下)

---

## 三、工时登记与进度计算(重点)

### 单位约定
- **1 人天 = 7.5 小时**(一个标准工作日)
- 计划/已用工时按「人天」展示,工时登记按「小时」录入(支持 0.5 步进)
- 项目「已用工作日」= 该项目累计登记工时 ÷ 7.5,自动折算并叠加显示(如登记 15 小时 → 已用 2 天);项目尚无登记时按日期推算

### 怎么记工时
**① 任务级(推荐,最细粒度)**:在任务卡片点 **「⏱ 记工时」** → 填工时数字(必填)、记录人(默认任务负责人)、备注 → 点 **「确认保存」** → 系统自动记录**登记日期时间**(精确到分钟),工时归属该任务并汇总到所属项目,任务右上角实时显示已登记小时数。

**② 项目级(快速登记)**:点顶部 **「记工时」** 按钮 → 选择项目、记录人(常用姓名可快捷点选)、日期、工时数、备注 → 保存。

### 怎么完成任务
任务卡片上的 **「完成」按钮**(绿色)代替了原来的勾选:点击即完成任务(状态变「已完成 ✓」),再点一次可恢复为待开始。所有视图(任务列表、项目详情)统一使用按钮形式。

### 登记工时做什么用(关键!)
| 你做的操作 | 系统自动发生的事 |
|-----------|----------------|
| 在任务上登记一笔工时(如张三 4 小时) | 该任务工时徽标更新,所属项目「工时进度 E」「综合进度 P」「已用工作日」立即重算并同步到后端 |
| **编辑一笔已登记工时**(点明细里的「编辑」) | 工时数/记录人/日期/备注/项目都可改,**登记时间不变**;改项目时新旧项目都重算 |
| 删除一笔误登记 | 工时进度 E、综合进度 P、已用工作日自动回退重算 |
| 项目无任何登记 | 工时进度 E 按 0 计算,综合进度只取时间进度 T,已用工作日按日期推算 |

> 例:某项目计划 10 人天 = 75 小时,已登记 30 小时 → 工时进度 E = 30 ÷ 75 = **40%**;综合进度 P = E×70% + T×30%;已用工作日 = 30 ÷ 7.5 = **4 天**。

---

## 四、进度怎么自动算(三档优先级)

每个项目打开「自动计算」后,进度按以下顺序取**第一个能算的**:

| 优先级 | 条件 | 公式 |
|-------|------|------|
| ① | 有工时登记 + 有计划工时 | **综合进度 = E×70% + T×30%** |
| ② | 无登记但有任务 | 任务完成率 = 已完成任务数 ÷ 任务总数 |
| ③ | 无登记无任务 | 时间进度 = (今天-开始) ÷ (截止-开始) |

其中:
- **E 工作量进度** = 登记工时 ÷ 计划工时 × 100%(封顶 100%,纯看干活量,和日期无关)
- **T 时间进度** = 已经过的天数 ÷ 总工期(仅作参考辅助)
- 三个指标在统计视图每个项目卡里同时展示

### 超支 / 滞后自动标记
| 指标 | 含义 | 标记 |
|------|------|------|
| 工时消耗率 > 100% | 实际投入已超过预估 | 🔴 超支 X% |
| E − T < −15 | 干活进度明显落后于时间 | 🟠 进度滞后 |
| E − T ≥ 15 | 干活进度明显快于时间 | 🟢 超前 |

---

## 五、项目管理(编辑弹窗)

新建/编辑项目时可设置:
- **基本信息**:名称、分类、状态(规划中/进行中/已交付/已暂停)、优先级 P0-P2、负责人、描述
- **时间**:开始日期、截止日期(截止仅作时间约束,不参与工时计算)
- **工作量**:计划工作日(自动模式 = 开始→截止的工作日数,不含周末,可手动改)、已用工作日(自动模式 = **登记工时 ÷ 7.5 折算**,项目尚无登记时按开始→今天的工作日数推算)
- **两个开关**:进度自动/手动、工时自动/手动,可独立切换
- **关键节点**:任意添加里程碑(名称 + 日期)
- **已登记工时**:弹窗内直接看该项目登记了 X 小时(≈Y 个工作日),点「查看明细」可逐笔核对(含登记时间),每条可**编辑**(改工时/人/日期/备注/项目)或删除误登记

---

## 六、数据怎么存、怎么同步

| 位置 | 说明 |
|------|------|
| 浏览器本地 | 每次操作立即写入 localStorage,断网也能用 |
| 后端服务器 | 联网时自动推送全量数据(项目+任务+工时),支持多设备同步 |
| 数据卷 | 部署在云端时数据存服务器持久化卷,重建服务不丢数据 |

**保存方式**:所有操作(新建/编辑/删除/拖拽排序/登记工时)都会**立即同步**到后端,无需手动点保存。

---

## 七、导出与备份

| 按钮 | 用途 |
|------|------|
| **Excel** | 在线导出 .xlsx(5 个工作表:项目总表/任务明细/关键节点/统计汇总/工时登记,工时表含项目/关联任务/人员/登记日期/登记时间/工时/折算工作日/备注);离线自动回退 CSV |
| **导出** | 导出完整 JSON 备份(含工时记录),用于数据搬家/恢复 |
| **导入** | 恢复 JSON 备份(会覆盖当前数据,需二次确认) |

---

## 八、日常操作路径速查

**场景 A:新接一个项目**
新建项目 → 填名称/分类/负责人/起止日期 → 计划工作日自动算好 → 建任务 → 开始登记工时

**场景 B:每天下班前 2 分钟**
进「任务」页 → 对今天干过的任务点「⏱ 记工时」→ 填数字 → 确认保存(自动记录日期时间)。或顶部「记工时」按项目快速登记。系统自动更新工时进度、综合进度与已用工作日。

**场景 C:周例会汇报**
打开统计视图 → 看人员排行谁忙谁闲、看项目卡有没有超支/滞后标红 → 点「Excel」一键导出发群里。

**场景 D:发现某项目进度异常**
看板点该项目卡片 → 查看「已登记工时」明细(逐笔含时间) → 对比「计划/已用工作日」→ 判断是登记漏了还是真的超支 → 补登记或调整计划。

---

## 九、当前版本与部署

- 版本徽章:页面标题旁蓝色 **v3.8** 徽章
- 本地文件:`研发项目工作台.html`(双击即可用,数据存本地)
- 线上地址(需重新部署后生效):Railway / CloudStudio
- 判断线上是否最新:看标题旁徽章是否为 v3.8

---

## 十、后续可扩展方向

- 工时登记按月/按周汇总报表
- 人员负载均衡建议(自动提示谁超负荷)
- 项目预算(金额口径)与工时双维度管理
- 审批流(登记超过预估自动提醒负责人)
